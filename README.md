# example4

I have prepared the Excel file above to simplify the gap review for the Statistical Programming team. As a suggestion, we could send this spreadsheet to Tamsin instead of the ARD field list, since it already maps the variables of interest, the available ARD variables, and the expected ADaM source, with only Column I requiring completion. We could start with ANTHEM and POWER first, as Pablo prioritized. What do you think?
