# example4

1. In the UC file that you attached it looks like a Y/N assessment has already been done for many of the studies. Therefore, is this request actually focused on providing guidance in the yellow highlighted final column regarding where to find equivalent data if it is not in the suggested datasets and variables?

Yes. The main purpose of this exercise is to complete the information in the yellow column by identifying whether the missing variables are unavailable in the received ADaM datasets or were not collected for the study. If the data are available elsewhere, please also identify the corresponding dataset and source variable.

2. Should the Statistical Programming review focus on variables in columns A, B and E only (not C or D)?

Yes. Please focus on the variables listed in columns A, B and E. Columns C and D are reference information (dataset/variable names identified in the current ARD). The review should focus on the rows where the variable is marked as "not available", confirming whether the data exist elsewhere in the study and, if so, where they can be found.

3. I noticed for the Quasar study a mixture of observed variables (e.g. ADHIST.NEUTPL) and imputed variables (e.g. ADHISTI.NEUTEP) listed. The imputed variables have additional considerations for intercurrent events and missing data rules. We don't recommend combining the 2 different approaches together. Was this selection intentionally different?

I'll confirm this point with the project team and get back to you.
